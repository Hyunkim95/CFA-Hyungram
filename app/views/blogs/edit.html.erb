<div class="text-center">
  <%= image_tag @blog.image.url(:medium) %>
</div>

<%= render 'partials/form' %>

<div class="text-center edit-links">
<%= link_to "Delete Post", blog_path(@blog), method: :delete, data: { confirm: "Are you sure you want to delete this post?" } %>
|
<%= link_to "cancel", blogs_path %>
</div>
